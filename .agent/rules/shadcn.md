---
trigger: always_on
description: when working on the UI
---

this project uses shadcn ui for all ui elements

if a particular shadcn component is not installed, use the following command (for example to install the button component)
npx shadcn@latest add button